<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.program.mapper.DtsAdminMapper">
    <resultMap id="BaseResultMap" type="com.ruoyi.program.entity.DtsAdmin">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="add_time" property="addTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="BIT"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="role_ids" property="roleIds" jdbcType="VARCHAR"/>
        <result column="last_login_ip" property="lastLoginIp" jdbcType="VARCHAR"/>
        <result column="last_login_time" property="lastLoginTime" jdbcType="TIMESTAMP"/>
        <result column="tel" property="tel" jdbcType="VARCHAR"/>
        <result column="mail" property="mail" jdbcType="VARCHAR"/>
    </resultMap>
    <insert id="insertadmin">
        insert into dts_admin (username, password, add_time, update_time, deleted, avatar, role_ids, last_login_ip,
                               last_login_time, tel, mail)
        values (#{username}, #{password}, #{addTime}, #{updateTime}, #{deleted}, #{avatar}, #{roleIds}, #{lastLoginIp},
                #{lastLoginTime}, #{tel}, #{mail})
    </insert>
    <update id="updateadmin">
        update dts_admin
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="addTime != null">add_time = #{addTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="roleIds != null">role_ids = #{roleIds},</if>
            <if test="lastLoginIp != null">last_login_ip = #{lastLoginIp},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="tel != null">tel = #{tel},</if>
            <if test="mail != null">mail = #{mail},</if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteById">
        delete
        from dts_admin
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <select id="selectadmin" resultMap="BaseResultMap">
        select id,
               username,
               password,
               add_time,
               update_time,
               deleted,
               avatar,
               role_ids,
               last_login_ip,
               last_login_time,
               tel,
               mail
        from dts_admin
    </select>
    <select id="getAdminById" resultMap="BaseResultMap">
        select id,
               username,
               password,
               add_time,
               update_time,
               deleted,
               avatar,
               role_ids,
               last_login_ip,
               last_login_time,
               tel,
               mail
        from dts_admin
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="getAdminByNameFuzzy" resultType="com.ruoyi.program.entity.DtsAdmin">
        SELECT id,
               username,
               password,
               add_time,
               update_time,
               deleted,
               avatar,
               role_ids,
               last_login_ip,
               last_login_time,
               tel,
               mail
        FROM dts_admin
        WHERE username LIKE CONCAT('%', #{username, jdbcType=VARCHAR}, '%')
    </select>


</mapper>