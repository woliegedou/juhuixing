<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.program.mapper.DtsArticleMapper">
    <resultMap id="BaseResultMap" type="com.ruoyi.program.entity.DtsArticle">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="add_time" property="addTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="BIT"/>
    </resultMap>
    <insert id="insertArticle">
        insert into dts_article
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="type != null">type,</if>
            <if test="title != null">title,</if>
            <if test="content != null">content,</if>
            <if test="addTime != null">add_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="deleted != null">deleted</if>
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="type != null">#{type},</if>
            <if test="title != null">#{title},</if>
            <if test="content != null">#{content},</if>
            <if test="addTime != null">#{addTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="deleted != null">#{deleted}</if>
        </trim>
    </insert>
    <update id="updateArticle">
        update dts_article
        <set>
            <if test="type != null">type = #{type},</if>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="addTime != null">add_time = #{addTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="deleted != null">deleted = #{deleted}</if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteArticle">
        delete
        from dts_article
        where id = #{id}
    </delete>
    <select id="selectArticleList" resultMap="BaseResultMap">
        select id,
               type,
               title,
               content,
               add_time,
               update_time,
               deleted
        from dts_article
    </select>
    <select id="selectArticleListFuzzyQuery" resultMap="BaseResultMap">
        select id,
               type,
               title,
               content,
               add_time,
               update_time,
               deleted
        from dts_article
        where title like concat('%', #{title}, '%')
    </select>


</mapper>